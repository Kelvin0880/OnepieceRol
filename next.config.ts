import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev-only "N" badge sits on top of the phone bottom sheets (and blocks clicks in the browser checks).
  devIndicators: false,
};

export default nextConfig;
